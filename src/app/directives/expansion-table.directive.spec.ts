import { ExpansionTableDirective } from './expansion-table.directive';

describe('ExpansionTableDirective', () => {
  it('should create an instance', () => {
    const directive = new ExpansionTableDirective();
    expect(directive).toBeTruthy();
  });
});
